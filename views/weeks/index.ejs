<%- include('../partials/head') %>
<%- include('../partials/header') %>
    
    <h1><%= page.title %></h1>

    <% weeks.forEach((week)=> { %>
        <div class="list">

            <h3><%= week.weekStart %></h3>
            <div class="form-control-buttons">
            <a href="/weeks/<%=week.id%>">
                View
            </a>
            </div>

        </div>
    <% }) %>
    
<%- include('../partials/footer') %>